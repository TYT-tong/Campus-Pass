## 推送内容概览
- 变更文件：`src/stores/permission.ts`
- 核心改动：
  - 解析 `component: "Layout"/"ParentView"` 并正确渲染到主容器。
  - 识别外链（`meta.link` 或 `path` 为 http(s)），在主容器通过 iframe 内嵌显示。
  - 首次加载菜单后，动态注册路由到 `vue-router`，侧边栏点击即可在右侧主容器呈现内容。

## 分支与目标
- 默认在新分支推送：`feature/dynamic-routes`（推荐独立分支便于评审与回滚）。
- 目标远程：`origin`（如未配置将按你提供的 URL 增加）。
- 可选目标主分支：`main` 或组织指定分支（若需直接推到主分支，可改用对应分支名）。

## 推送步骤
1. 检查仓库状态
   - `git status`
   - `git remote -v`
2. 设置用户信息（若首次提交）
   - `git config user.name "你的名字"`
   - `git config user.email "你的邮箱"`
3. 创建并切换到工作分支
   - `git checkout -b feature/dynamic-routes`
4. 暂存改动
   - `git add src/stores/permission.ts`
5. 编写提交信息并提交
   - `git commit -m "feat(router): 支持 RuoYi 菜单动态路由与外链 iframe"`
6. 若未配置远程（任选其一）
   - `git remote add origin <你的远程仓库URL>`
7. 推送到远程并建立追踪
   - `git push -u origin feature/dynamic-routes`
8. （可选）创建合并请求/拉取请求
   - 在平台（GitHub/GitLab/Gitee）以此分支发起 PR/MR，标题可用同提交信息。

## 验证与回滚
- 推送成功后，确认 CI/CD 或构建管线是否通过。
- 若需回滚：
  - `git revert <commit_sha>` 生成回滚提交；或关闭/回退 PR。

## 需要你确认的信息
- 远程仓库地址（如本地未配置）。
- 目标分支（默认 `feature/dynamic-routes`，或指定主分支名）。

请确认以上计划和远程信息，确认后我将按步骤执行推送。