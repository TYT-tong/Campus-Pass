## 目标
- 查找并终止占用本机 `9202` 端口的进程，释放端口。

## 执行步骤
- 查询端口占用并直接终止监听该端口的进程：
  - 使用 PowerShell 获取本机 `9202` 端口的连接，并对其 `OwningProcess` 执行强制终止。
- 验证端口状态：使用 `netstat -ano | findstr :9202` 确认无监听。

## 影响
- 仅结束占用 `9202` 的进程（当前为 cp-system 的 Tomcat 进程），不会影响其它端口。

## 验证
- 输出为空表示端口已释放。