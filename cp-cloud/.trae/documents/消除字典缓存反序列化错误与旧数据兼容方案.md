## 目标
- 将实体类的显式 getter/setter 改为 Lombok 注解生成，统一风格、减少模板代码。
- 同步修复 `SysDictData` 旧字段兼容问题，避免 Redis 旧缓存中的 `default` 字段反序列化报错。

## 影响范围
- 模块：`cp-api/cp-api-system` 下的 `com.cp.system.api.domain.*`
  - 主要类：`SysDictData`、`SysDictType`、`SysUser`、`SysRole`、`SysLogininfor`
- 兼容：`BaseEntity` 与 MyBatis-Plus 注解保留；`@EqualsAndHashCode(callSuper = true)` 保持。

## 实施步骤
1) 启用 Lombok（项目已使用）
- 在上述实体类上统一使用：`@Data`（或 `@Getter/@Setter` 组合）+ `@EqualsAndHashCode(callSuper = true)`（继承自 `BaseEntity` 的类）。
- 移除显式 getter/setter 方法。
- 将 `@NotBlank/@Size` 等校验注解从 getter 移到字段上，保持校验行为。

2) 旧字段兼容（SysDictData）
- 在 `isDefault` 字段上增加：`@JsonAlias("default")`，使 Jackson 能识别旧缓存中的 `default` 字段。
- 保留 `@TableField(value = "is_default")` 与导出注解。
- 如存在 `getDefault()` 业务方法，保留（用于将 `isDefault` 的 Y/N 转布尔）。

3) 代码一致性
- 保留所有 MyBatis-Plus 注解（`@TableId/@TableField`）与导出注解（`@Excel` 等）。
- 保留序列化标识（`serialVersionUID`）。

## 验证
- 编译并启动 `cp-system`。
- 访问 `/dict/data/type/sys_user_sex` 等接口：不再出现 `Unrecognized field "default"` 与 `Unrecognized token 'Set'`。
- 登录与受保护接口读取 Redis 的 `LoginUser` 与字典缓存正常。

## 交付
- 更新以上实体类为 Lombok 风格，并提交修改。

请确认后我将按以上步骤实施并验证。